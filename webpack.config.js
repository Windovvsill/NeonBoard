// import TsconfigPathsPlugin from "tsconfig-paths-webpack-plugin";

// export const resolve = {
//   plugins: [new TsconfigPathsPlugin({})],
// };

// webpack config can not be used unless the react-scripts project it ejected
